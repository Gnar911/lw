# lw

This folder contains the `lw` library sources and utilities used by the workspace.

Files added:

- `.gitignore` — ignores common unwanted files and the literal entry `unecessary` per request.

If you want an initial commit created, tell me and I'll run it for you.
